@page "/reader/{BookId}"

<ReadView BookId=@GetBookIdInt()></ReadView>

@code {
	[Parameter]
	public string? BookId { get; set; }

	protected override void OnParametersSet()
	{
		StateHasChanged();
	}

	int GetBookIdInt()
	{
		return int.Parse(BookId);
	}
}